<%- "A".upto("Z") do |l| -%>
    <%= link_to_unless (@tag == l), l, list_public_bodies_path(:tag => l.downcase) %>
<% end %>

<%- ['Æ', 'Ø', 'Å'].each do |l| -%>
    <%= link_to_unless (@tag == l), l, list_public_bodies_path(:tag => l.downcase) %>
<% end %>
